# MySQL connection string (local dev) - note: your MySQL runs on port 3307
DATABASE_URL="mysql://root:@127.0.0.1:3307/next_prisma_crud"

# Example for SQLite (zero-config, no server needed):
# DATABASE_URL="file:./dev.db"

